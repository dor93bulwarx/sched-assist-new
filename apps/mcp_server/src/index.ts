/**
 * MCP server entry point — placeholder for future implementation.
 * Runs in its own container alongside the agent_service.
 */

console.log("[mcp_server] Starting…");
console.log("[mcp_server] Ready on port 3002.");

// Keep the process alive (replace with actual server in later milestone).
setInterval(() => {}, 60_000);
